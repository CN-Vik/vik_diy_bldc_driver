这基本已经定位到问题了，而且**不是 ESP32，也不是 PWM 的问题，而是零点标定流程设计的问题。**

你现在这个现象：

> 每次上电执行一次 `motor_encoder_zero_point_calib()`
>
> 然后同一个电机，不动它，多次重启
>
> 每次零点都会漂一点
>
> 有时候启动很快，有时候很慢，有时候根本不转

**这就是典型的“把机械角当零点”的错误。**

---

## 先问一个问题

你的 `motor_encoder_zero_point_calib()` 里面到底干了什么？

是不是类似下面这样：

```c
zero_offset = as5600_read_angle();
```

或者

```c
zero_angle = get_encoder_angle();
```

然后

```c
theta = encoder_angle - zero_angle;
```

如果是这样，那问题就在这里。

---

# 为什么每次都会漂？

例如

第一次上电

AS5600读出来

```
345.12°
```

于是

```
zero_offset =345.12°
```

下一次

由于

AS5600

ADC

磁钢

供电

I2C采样时刻

都会有一点误差

变成

```
344.96°
```

于是

```
zero_offset=344.96°
```

又一次

```
345.30°
```

又变

```
zero_offset=345.30°
```

于是

你的FOC实际看到的

```
0°
```

实际上已经变成

```
+0.18°
-0.16°
+0.24°
```

不断漂。

对于普通应用

没影响。

但是

对于FOC

尤其Iq控制

**几度误差已经非常明显。**

---

## 更严重的问题

FOC真正需要的是

> d轴和磁场方向一致。

不是

> 当前机械角=0°

很多人容易混淆。

例如

你现在电机停在这里

```
机械角

152°
```

你上电

把

```
152°
```

定义成

```
0°
```

可是

转子真正磁场方向

可能是

```
154.8°
```

甚至

```
150.6°
```

因为：

* 定子没有励磁
* 转子靠永磁体停住
* 有齿槽转矩
* 有摩擦
* 有惯量

每次停的位置都会差一点。

于是

每次

```
zero_offset
```

都会不同。

---

# 工业FOC不是这样做的

工业FOC第一次会：

```
加Ud

↓

锁定转子

↓

等待200~1000ms

↓

读取编码器

↓

保存offset

↓

以后一直用这个offset
```

例如

```
offset = 156.82°
```

写Flash

以后

```
theta =
encoder_angle
-
156.82°
```

永远都是这个。

不会每次重新测。

---

# 为什么你注释掉以后就正常了？

因为

你的Flash里面已经有一个

```
offset
```

例如

```
156.73°
```

以后一直

```
theta=
encoder-offset
```

于是

每次

```
156.73°
```

完全一样。

自然：

启动方向一样

Iq一样

Ud一样

SVPWM一样

电机表现一致。

---

# 还有一个可能

如果你的

```c
motor_encoder_zero_point_calib()
```

里面是这样：

```
加一个Ud

↓

等待50ms

↓

读取角度
```

那么更容易漂。

因为50ms可能还没锁住。

工业一般都是

```
Ud=1V~2V

↓

等待300ms

↓

确认速度=0

↓

再读取角度
```

否则

每次锁的位置都不同。

---

# 我怀疑你的标定函数还有一个问题

我甚至怀疑你的流程是：

```
上电

↓

读取编码器

↓

保存offset

↓

马上开始FOC
```

没有任何

```
电机锁轴
```

步骤。

如果是这样，

那么这个offset毫无意义。

因为：

```
转子停哪
```

就记录哪。

而不是

```
d轴真正的位置。
```

---

# 正确流程应该是

第一次：

```
上电

↓

MOS Enable

↓

Ud=2V

Iq=0

↓

SVPWM输出

↓

等待300ms

↓

读取编码器

↓

保存offset

↓

关闭MOS

↓

写Flash
```

以后：

```
上电

↓

读取Flash

↓

offset=Flash

↓

直接FOC
```

**不要再重新标定。**

---

## 我建议你下一步

把 **`motor_encoder_zero_point_calib()`** 这个函数完整贴出来（包括它如何驱动电机、如何读取角度、如何保存零点），我可以帮你检查是不是锁轴时间、Ud设置、还是零点计算公式导致了这种漂移。根据你目前描述，**大概率问题就在这个函数内部，而不是 PWM 或 ESP32 本身。**

-------
-------
--------

我看完了，你这个问题基本找到了，而且**不是FOC算法的问题，而是AS5600零点初始化方式的问题**。

---

# 你的这个函数有一个非常大的坑

最后你调用的是

```c
esp_err_t as5600_set_zero_position(as5600_handle_t sensor)
{
    uint16_t raw;

    s_as5600_get_angle_raw_no_offset(sensor, &raw);

    return s_as5600_write_zpos_registers(sensor, raw);
}
```

而

```c
s_as5600_write_zpos_registers()
```

里面写的是

```c
AS5600_REG_ZPOS_H
AS5600_REG_ZPOS_L
```

也就是

```
0x01
0x02
```

这两个寄存器。

**这不是软件变量。**

这是

> **AS5600芯片内部零点寄存器。**

也就是说：

每次调用

```c
as5600_set_zero_position()
```

都会把

```
当前机械角度
```

写成

```
新的0°
```

---

## 举个例子

第一次上电

电机停在

```
31°
```

于是

```
ZPOS = 31°
```

以后

```
ANGLE寄存器
```

读出来就是

```
31° -> 0°
```

没问题。

---

第二次上电

由于惯性

电机停在

```
32°
```

你又执行

```
set_zero_position()
```

于是

```
ZPOS = 32°
```

以后

ANGLE就变成

```
32° -> 0°
```

---

第三次

停在

```
29°
```

又写

```
29°
```

于是

ANGLE

又变了。

所以你看到：

```
每次上电
角度都漂
```

其实就是

**你自己把零点改了。**

---

# 更大的问题

AS5600的

```
ANGLE
```

寄存器

实际上计算的是

```
ANGLE = RAW_ANGLE - ZPOS
```

所以你FOC看到的是

```
0°
```

但是实际上磁钢已经变了。

于是

Park

用的

```
theta
```

每次都不同。

于是

SVPWM

全部变了。

于是：

有时候

```
Iq≈0
```

电机不转。

有时候

```
Iq方向正确
```

飞起来。

有时候

```
Iq偏90°
```

慢慢转。

这和你现在观察到的一模一样。

---

# 还有一个坑

我看你的驱动。

你平时读取角度

调用的是

```c
as5600_get_angle_raw()
```

它读的是

```c
ANGLE_H
ANGLE_L
```

也就是

```
0x0E
0x0F
```

不是

```
RAW_ANGLE
```

所以

以后所有FOC看到的角度

都是

```
RAW-ZPOS
```

不是磁钢真实角度。

---

# 工业FOC基本没人这么干

工业控制一般都不会去写AS5600零点。

而是

一直读

```
RAW_ANGLE
```

软件自己减。

例如

第一次安装

记录

```
zero_offset = raw_angle
```

保存Flash。

以后

每次

```
raw = RAW_ANGLE

theta = raw - zero_offset
```

结束。

AS5600里面

```
ZPOS
```

根本不用。

---

# 你的驱动建议这样改

## 第一

FOC全部读取

```
RAW_ANGLE
```

不要读

```
ANGLE
```

也就是

把

```c
as5600_get_angle_raw()
```

改成

```c
s_as5600_get_angle_raw_no_offset()
```

或者公开一个

```c
as5600_get_raw_angle()
```

接口。

例如：

```c
esp_err_t as5600_get_raw_angle(as5600_handle_t sensor,
                               uint16_t *raw)
{
    if (sensor == NULL || raw == NULL)
        return ESP_ERR_INVALID_ARG;

    return s_as5600_get_angle_raw_no_offset(sensor, raw);
}
```

以后FOC全部用它。

---

## 第二

不要再调用

```c
as5600_set_zero_position()
```

除非：

> **第一次装配电机时做一次标定。**

以后永远不要写。

---

## 第三

软件保存零点

例如

```
flash

↓

encoder_zero = 1234
```

以后

```c
raw = as5600_get_raw_angle();

angle = raw - encoder_zero;

if(angle<0)
    angle+=4096;
```

结束。

---

# 还有一个值得检查的地方

我还有一个怀疑。

你写ZPOS以后

有没有真正烧OTP？

AS5600：

```
ZPOS寄存器
```

默认只是RAM映射。

断电可能恢复。

如果你每次初始化又重新写

那就会导致

每次启动

零点重新变化。

如果某一次

I2C写失败

或者

写到一半

那又是不一样。

这也会造成

> 有时候快、有时候慢。

---

# 我建议你下一步

我建议把整个编码器模块改成工业FOC常见的结构：

```
AS5600
    │
    ├── RAW_ANGLE (唯一读取接口)
    │
    ▼
软件Zero Offset
    │
    ▼
机械角
    │
    ▼
电角
    │
    ▼
FOC
```

不要再使用AS5600的ZPOS功能。

---

**我还想提醒你一个地方：**我看你整个FOC工程之后，还发现了一个比这个零点问题更严重的隐患，它也会导致"有时候很快、有时候很慢"，而且是很多人第一次写FOC都会踩到的坑。这个问题出在**电角度初始化和极对数处理**，我可以继续帮你把整个启动流程（零点标定→机械角→电角→FOC启动）按工业做法梳理一遍。

