明白了，是我刚才理解错了。

你的架构应该是这样的：

```text
                Core0                                  Core1

20kHz PWM ISR
      │
      ├── notify
      ▼
FOC Task(20kHz)
      │
      ├── 读取 ia ib ic（DMA最新值）
      ├── 读取 theta（共享变量）
      ├── Clarke
      ├── Park
      ├── PI
      ├── 反Park
      ├── SVPWM
      └── 更新PWM
                                                ADC DMA一直20kHz采样
                                                     │
                                                     ▼
                                              电流采样Task(20kHz)
                                                     │
                                              更新 ia ib ic

                                                编码器Task(1kHz)
                                                     │
                                              更新 theta
```

如果是这样的话，那么：

* **电流采样20kHz**，基本没问题。
* **FOC 20kHz**，也没问题。

---

## 但是！！

我现在一下子看到了一个真正可疑的地方。

### **编码器只有1kHz！！**

你前面一直强调：

> **角度读取是1kHz任务。**

这个我之前忽略了。

---

FOC是20kHz。

也就是说：

```text
20次FOC

↓

才更新一次角度
```

也就是：

```text
第1次FOC
theta=100°

第2次
100°

第3次
100°

...

第20次
100°

下一毫秒

theta=101°
```

如果电机在转，

这是绝对不行的。

---

但是！！

你现在测试的是

> **堵转。**

堵转的时候：

```text
theta

应该完全不变。
```

所以

**1kHz读取角度本身不会导致堵转Iq正负翻转。**

因此，

**编码器更新频率不是你现在这个问题的根因。**

---

# 那现在还剩什么？

我重新整理了一遍。

如果下面全部成立：

✅ Clarke公式正确

✅ Park公式正确

✅ 反Park正确

✅ SVPWM正确

✅ 电流Offset正确

✅ 电角度计算正确

✅ 上电零点固定

✅ 电流20kHz采样

那么：

**Iq不可能随机变号。**

这是FOC数学本身决定的。

---

# 所以现在我反而怀疑"数据一致性"

因为你是双核。

Core1一直写：

```c
ia
ib
theta
```

Core0一直读：

```c
ia
ib
theta
```

请问：

有没有任何同步？

例如：

```c
volatile float ia;
volatile float ib;
volatile float theta;
```

或者

```c
portENTER_CRITICAL()
```

或者

```c
spinlock
```

如果没有。

那么完全可能发生：

例如：

Core1

```text
更新ia
```

此时

Core0

```text
读到了新的ia
```

然后

Core1

```text
还没来得及更新ib
```

Core0

```text
又读到了旧ib
```

于是：

```text
ia

来自第101次采样

ib

来自第100次采样
```

然后

```text
ic=-ia-ib
```

整个电流矢量方向直接变了。

Park以后，

Iq完全可能翻符号。

---

## 我想问你一个问题

Core1是不是这样写？

```c
vfoc_dt.ia = xx;
vfoc_dt.ib = xx;
vfoc_dt.theta = xx;
```

Core0就是直接：

```c
ia = vfoc_dt.ia;
ib = vfoc_dt.ib;
theta = vfoc_dt.theta;
```

如果是这样，

**没有任何保护的话，我觉得这是目前最大的嫌疑。**

---

# 我建议你马上做一个实验

不要改任何算法。

只改这一件事。

在FOC Task开始的时候，一次性把共享变量拷贝到局部变量：

```c
float ia;
float ib;
float theta;

taskENTER_CRITICAL();
ia = vfoc_dt.ia;
ib = vfoc_dt.ib;
theta = vfoc_dt.theta;
taskEXIT_CRITICAL();
```

后面整个FOC都只使用：

```c
ia
ib
theta
```

不要中途再去调用：

```c
get_vfoc_theta()
get_vfoc_ia()
```

如果这样以后，

**Iq突然稳定了。**

那说明不是Park。

就是**双核共享数据竞争（Data Race）**。

---

## 最后还有一个我一直想确认的问题

你堵转的时候打印的那个：

```c
ESP_LOGI(...,
    iq,
    theta...
);
```

**`iq` 和 `theta` 是不是来自同一次FOC计算？**

还是说：

```text
先算出了iq

↓

ESP_LOGI里面又调用了一次

get_vfoc_theta_e_rad()

重新读共享变量
```

如果是第二种，

**日志里的Iq和角度甚至都不是同一时刻的数据。**

这个细节虽然不一定是根因，但会严重干扰调试判断。


-----------
---------
--------
**我觉得我们已经快定位到了。**

你补充的信息，把很多可能性都排除了。

目前我认为嫌疑最大的已经不是 Park，也不是 Clarke，而是**双核共享数据的一致性**或者**UVW 与采样相位对应关系**。

---

## 先说双核共享变量

你现在是：

```text
Core1
20kHz：
ia = ...
ib = ...

Core1
1kHz：
theta = ...

-------------------------

Core0
20kHz：
ia = 全局变量
ib = 全局变量
theta = 全局变量

Clarke
Park
PI
SVPWM
```

这里最大的风险不是 `volatile`，而是**一致性**。

例如：

```
时刻 T0
ia = 1.00
ib = -0.50

Core0开始读
ia = 1.00

------------ CPU切换 ------------

Core1更新

ia = 0.95
ib = -0.45

------------ CPU切换 ------------

Core0继续

ib = -0.45
```

于是你这一次FOC拿到的是：

```
ia = 1.00（旧）
ib = -0.45（新）
```

这种组合实际上从来没有存在过。

虽然 float 在 ESP32 上单次读写一般是原子的，但是**多变量组合不是原子的**。

---

## 但是！

**这还不能完全解释你的现象。**

为什么？

因为即使偶尔读到"新旧混合"的数据，

**也不应该导致：**

```
堵转1
Iq = +0.40A

堵转2
Iq = -0.40A
```

这种**整体翻转180°**。

它更像是：

```
0.39

0.36

0.41

0.35
```

不会直接变成负。

所以我认为：

**数据竞争值得修，但它不是第一嫌疑。**

---

# 我现在第一嫌疑变成了这个（★★★★★）

## 你的PWM输出相序，和ADC采样相序，到底是不是一一对应？

我举个例子：

你软件认为：

```
PWM_U ----> Motor A

PWM_V ----> Motor B

PWM_W ----> Motor C
```

而ADC：

```
ADC0 ----> Motor B电流

ADC1 ----> Motor C电流
```

但是代码写成：

```c
ia = ADC0;
ib = ADC1;
```

这样：

数学公式全对。

Park全对。

SVPWM全对。

但是

**Iq方向就是会错。**

---

## 我为什么越来越怀疑这个？

因为你之前说过一句：

> **三相线序我也不确定。**

这一句其实很关键。

如果：

```
PWM定义

A B C
```

而实际接线：

```
A C B
```

那么：

Park出来：

```
Iq

可能整体翻转。
```

---

# 还有一个实验，我强烈建议你做

## 固定角度锁轴

不要速度环。

不要电流环。

就：

```c
Ud = 2.0;
Uq = 0;
theta = 0°
```

锁轴。

然后打印：

```
Ia
Ib
Ic
Iq
Id
```

接着：

把 theta 改成：

```
90°

180°

270°
```

每次锁住。

观察：

```
Iq
```

是不是：

```
一直正

或者

一直负
```

还是：

```
90°正

180°负

270°正
```

这个实验能把**角度方向、相序、采样对应关系**一下子分开。

---

# 我还有最后两个问题

这两个问题，我觉得可能直接决定答案。

### **问题1**

你的编码器安装方向。

例如：

**手顺时针转电机。**

打印：

```
theta
```

是：

```
10

20

30

40
```

还是：

```
350

340

330
```

也就是说：

**机械顺时针，角度是增加还是减少？**

---

### **问题2（最重要）**

你现在说：

> **有时候重新上电就正常。**

我想问：

**重新上电以后，你的PWM三相初始化状态完全一样吗？**

例如：

是不是有：

```c
mcpwm_enable();
```

之前，

PWM输出有几十微秒随机状态？

或者：

SVPWM第一次输出：

```
Ua
Ub
Uc
```

不是：

```
50%

50%

50%
```

而是随机占空比？

因为如果第一次励磁方向随机，

再加上没有执行一次**固定的锁轴校准**，

就可能出现每次启动时转矩方向不同。

---

## 我目前的怀疑排序（按概率）

1. **★★★★★ PWM三相定义 与 ADC采样相定义 不一致。**
2. **★★★★★ 电机实际UVW接线 与 软件UVW定义 不一致。**
3. **★★★★☆ 双核共享变量没有做一致性快照（建议修复，但不像根因）。**
4. **★★★☆☆ 编码器方向与FOC正方向相反。**

---

**最后我想确认一个细节：**

> 你现在的 `Uq=3.5V` 是**开环电压模式**（直接给固定 `Uq`），还是已经进入了 **Iq 电流闭环**（PI 输出 `Uq`）？

这个区别很重要，因为如果是开环电压模式，很多闭环因素都可以先排除，定位会更快。

-----
----
-----

**卧槽，这句话非常关键！**

> **"PWM CH和ADC CH的对应关系我没关注过，这个还真是随便写的。"**

**我现在觉得，你先别怀疑 Park、Clarke、PID 了。先把这个搞清楚。**

---

## FOC有一个绝对不能错的前提

假设软件定义：

```text
PWM_U  -> U相绕组
PWM_V  -> V相绕组
PWM_W  -> W相绕组
```

那么ADC必须对应：

```text
ADC_IA -> U相电流
ADC_IB -> V相电流
ADC_IC -> W相电流
```

例如：

```c
// PWM
PWM0A -> U
PWM0B -> V
PWM1A -> W

// ADC
ia = ADC_CH0;   // 必须测U相
ib = ADC_CH1;   // 必须测V相
ic = -ia-ib;    // 推导W相
```

**这是一一对应的。**

---

## 如果你写成了

例如PCB实际上：

```text
INA240_A ---- 测的是V相
INA240_B ---- 测的是W相
```

结果代码：

```c
ia = INA240_A;
ib = INA240_B;
```

Park一直认为：

```text
ia = U相
ib = V相
```

实际上却是：

```text
ia = V相
ib = W相
```

那么：

* Clarke还是会算出一个αβ电流
* Park也能算出Id/Iq

**但是这个Iq已经不是"真正的转矩电流"了。**

所以你会看到：

```text
堵转

有时候+

有时候-

```

甚至：

```text
Uq增加

Iq反而减小
```

这种现象。

---

# 还有PWM也是一样

例如软件：

```text
SVPWM

Ua

Ub

Uc
```

最后输出：

```text
PWM0A -> U

PWM0B -> V

PWM1A -> W
```

但是电机线：

```text
U -> 电机黄

V -> 电机绿

W -> 电机蓝
```

如果你后来为了让电机转：

交换过两根线：

```text
黄

蓝

绿
```

那么ADC也必须对应调整。

否则：

```text
PWM认为

这是U

ADC认为

还是以前那个U
```

整个坐标系就错了。

---

# 我建议你现在先不要调算法。

先画一个表。

例如：

| 软件变量   | 实际硬件       |
| ------ | ---------- |
| PWM_U  | 哪一路MOS？    |
| PWM_V  | 哪一路MOS？    |
| PWM_W  | 哪一路MOS？    |
| ADC_IA | 测哪一路MOS电流？ |
| ADC_IB | 测哪一路MOS电流？ |

然后继续：

| MOS桥 | 接电机哪根线 |
| ---- | ------ |
| U桥   | 黄      |
| V桥   | 蓝      |
| W桥   | 绿      |

这两个表一对应。

**你就知道：**

```text
ia

到底是不是PWM_U对应的那一相。
```

---

# 为什么工业FOC第一次调试都要做这个？

因为：

**软件里的U/V/W其实只是名字。**

真正重要的是：

```text
PWM_U

↓

驱动的是哪一个MOS桥

↓

那个MOS桥流过的电流

是不是ADC_IA测的

↓

那个MOS桥接的是电机哪根线
```

只要这四个不是同一个相。

Park以后：

```text
Iq

一定不正常。
```

---

# 我建议你下一步不要继续猜

按照下面这个顺序检查：

**① 确认PWM_U、PWM_V、PWM_W分别驱动哪三个半桥。**

**② 确认每个INA240（或采样电阻）到底测的是哪个半桥的电流。**

**③ 确认代码里的 `ia`、`ib` 对应的是哪两个采样通道。**

**④ 最后再确认电机三根线分别接到了哪个半桥。**

---

## 我现在甚至怀疑，你目前的软件里可能存在这种情况：

```text
PWM_U
        ↓
      电机U

ADC_IA
        ↓
      实际测的是V相
```

如果真是这样，**你的Park公式即使100%正确，Iq也不会正确。**

---

### 我建议你先回答我一个问题：

**你的驱动板上有三个半桥（U/V/W）和两个电流采样电阻，对吗？**

如果是的话，请告诉我：

* **ADC_CH0（或 `mtor_ia_current`）这个采样电阻，物理上接在哪个半桥下面？**
* **ADC_CH1（或 `mtor_ib_current`）这个采样电阻，又接在哪个半桥下面？**

**这个是PCB的连接关系，不是代码变量名。** 一旦这两个关系明确了，我们基本就能判断 `ia/ib` 和 PWM 三相是否对应。
